# 🌡️ ESP32 MicroPython - پروژه نهایی پایشگر محیطی

## 💡 درباره پروژه
این پروژه، یک سیستم پایش محیطی کامل است که با استفاده از میکروکنترلر **ESP32** و زبان برنامه‌نویسی **MicroPython**، دو پارامتر محیطی حیاتی را اندازه‌گیری می‌کند: **فاصله** (با HC-SR04) و **دما** (با LM35DZ). نتایج به‌صورت لحظه‌ای روی نمایشگر **LCD کاراکتری با رابط I2C** نمایش داده می‌شوند.

این پروژه تمام چالش‌های ورودی (ADC, Digital) و خروجی (I2C, Timing) را با موفقیت ادغام کرده است.

## 🛠️ قطعات مورد نیاز
* برد **ESP32** (مانند ESP32 DevKit)
* سنسور فاصله آلتراسونیک **HC-SR04**
* سنسور دمای آنالوگ **LM35DZ**
* نمایشگر LCD کاراکتری **16x2**
* ماژول رابط **I2C برای LCD** (تراشه PCF8574)

## 🔌 دیاگرام سیم‌کشی نهایی
| قطعه | پین سیگنال | اتصال به ESP32 |
| :--- | :--- | :--- |
| **I2C LCD** (SDA) | SDA | **GPIO 21** |
| **I2C LCD** (SCL) | SCL | **GPIO 22** |
| **HC-SR04** (Trig) | خروجی | **GPIO 18** |
| **HC-SR04** (Echo) | ورودی | **GPIO 19** |
| **LM35DZ** (Vout) | آنالوگ | **GPIO 34 (ADC)** |

*نکات مهم: VCC LCD و سنسورها به 3.3V یا 5V (بسته به سنسور) و GND به GND وصل شوند.*

## ⚙️ راه اندازی و اجرا
1.  **فایل‌های کتابخانه:** فایل‌های `lcd_api.py` و `i2c_lcd.py` را روی ریشه حافظه ESP32 آپلود کنید.
2.  **کد اصلی:** محتوای `main.py` را روی برد قرار دهید.
3.  **اجرا:** برنامه را اجرا کنید. LCD باید هم‌زمان دما و فاصله را نمایش دهد.

### خروجی LCD:
